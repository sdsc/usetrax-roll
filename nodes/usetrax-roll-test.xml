<?xml version="1.0" standalone="no"?>

<kickstart>

<description>
The usetrax roll installation test.
</description>

<copyright>
Copyright (c) 2000 - 2011 The Regents of the University of California.
All rights reserved. Rocks(r) v5.1 www.rocksclusters.org
</copyright>

<changelog>
</changelog>

<post>

/bin/mkdir -m 0755 /root/rolltests

<file name="/root/rolltests/usetrax.t" perms="0755">
<![CDATA[#!/usr/bin/perl -w
# usetrax roll installation test.  Usage:
# usetrax.t [nodetype]
#   where nodetype is one of "Compute", "Dbnode", "Frontend" or "Login"
#   if not specified, the test assumes either Compute or Frontend

use Test::More qw(no_plan);

my $appliance = $#ARGV >= 0 ? $ARGV[0] :
                -d '/export/rocks/install' ? 'Frontend' : 'Compute';
my $installedOnAppliancesPattern = 'Frontend';
my $isInstalled = -d '/opt/usetrax';
my $output;

# usetrax-install.xml
if($appliance =~ /$installedOnAppliancesPattern/) {
  ok($isInstalled, 'usetrax installed');
} else {
  ok(! $isInstalled, 'usetrax not installed');
}
]]>
</file>

</post>

</kickstart> 
